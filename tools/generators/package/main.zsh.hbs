#!/usr/bin/env ksh
# -*- coding: utf-8 -*-

function hacker::pkg::{{lowerCase packageName}}::factory {
    # shellcheck source=/dev/null
    source "${ZSH_HACKER_SRC_PATH}"/pkg/{{lowerCase packageName}}/base.zsh
    case "${OSTYPE}" in
    darwin*)
        # shellcheck source=/dev/null
        source "${ZSH_HACKER_SRC_PATH}"/pkg/{{lowerCase packageName}}/osx.zsh
        ;;
    linux*)
        # shellcheck source=/dev/null
        source "${ZSH_HACKER_SRC_PATH}"/pkg/{{lowerCase packageName}}/linux.zsh
      ;;
    esac
}

hacker::pkg::main::{{lowerCase packageName}}::factory